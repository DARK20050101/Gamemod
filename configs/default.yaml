# Default configuration for game-automation
# Override by passing --config path/to/your.yaml

device:
  serial: "auto"
  width: 1080
  height: 1920

agent:
  max_steps: 100
  step_interval: 1.0
  screenshot_interval: 0.5

log:
  level: "INFO"
  file: null
  rotation: "10 MB"

adapters: {}

extra: {}
